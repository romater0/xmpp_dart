name: xmpp_stone

description: "Xmpp Stone is lightweight XMPP client library written completely in Dart."

version: 0.3.3

homepage: https://vukoye.github.io

environment:
  sdk: '>=2.7.0 <3.0.0'

#dependencies:
#  path: ^1.4.1
dependencies:
  console: 
    git:
      url : https://github.com/mzdm/console.dart.git
      ref : nnbd
  crypto: ^3.0.0
  cryptoutils: ^0.4.0
  image: ^3.0.2
  intl: ^0.17.0
  quiver: ^3.0.0
  synchronized: ^3.0.0
  tuple: ^2.0.0
  unorm_dart: ^0.2.0
  xml: ^5.0.2

dev_dependencies:
  mockito: ^4.0.0
  test: ^1.0.0
